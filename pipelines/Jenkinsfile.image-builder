#!/usr/bin/env groovy

pipeline {
    agent {
        label 'python'
    }
    
    options {
        buildDiscarder(logRotator(daysToKeepStr: '30', numToKeepStr: '20'))
        timeout(time: 2, unit: 'HOURS')
        timestamps()
        ansiColor('xterm')
    }
    
    environment {
        DOCKER_REGISTRY = "docker.io"
        DOCKER_PROJECT = "jenkins"
        JENKINS_VERSION = "${JENKINS_VERSION}"
        AGENT_VERSION = "${JENKINS_AGENT_VERSION}"
        BUILD_DATE = sh(script: 'date -u +"%Y-%m-%dT%H:%M:%SZ"', returnStdout: true).trim()
        GIT_COMMIT = sh(script: 'git rev-parse HEAD', returnStdout: true).trim()
        IMAGE_TAG = "${(params.IMAGE_TAG != 'latest') ? params.IMAGE_TAG : env.BUILD_NUMBER}"
        DOCKER_BUILDKIT = '1'
        COMPOSE_DOCKER_CLI_BUILD = '1'
    }
    
    stages {
        stage('Preparation') {
            steps {
                script {
                    echo "🚀 Jenkins Infrastructure Image Builder Pipeline"
                    echo "📦 Docker Registry: ${DOCKER_REGISTRY}/${DOCKER_PROJECT}"
                    echo "🏷️ Image Tag: ${IMAGE_TAG}"
                    echo "🔨 Force Rebuild: ${params.FORCE_REBUILD}"
                    echo "📤 Push to Registry: ${params.PUSH_TO_REGISTRY}"
                    echo "🎯 Images to Build: ${params.IMAGES_TO_BUILD}"
                    
                    // Docker registry login (optional for public registry)
                    echo "📝 Using public Docker registry (no login required)"
                }
            }
        }
        
        stage('Build Images with Ansible') {
            steps {
                script {
                    echo "🏗️ Building Jenkins Images using Ansible jenkins-images role..."
                    
                    // Prepare ansible variables for image building
                    def extraVars = [
                        "jenkins_master_image_tag=${IMAGE_TAG}",
                        "jenkins_agent_image_tag=${IMAGE_TAG}",
                        "jenkins_images_force_rebuild=${params.FORCE_REBUILD}",
                        "jenkins_images_push=${params.PUSH_TO_REGISTRY}"
                    ]
                    
                    // Execute ansible playbook
                    sh """
                        echo "🚀 Executing Ansible playbook to build images..."
                        ansible-playbook -i ansible/inventories/production/hosts.yml \
                            ansible/deploy-images.yml \
                            --extra-vars '${extraVars.join(' ')}' \
                            --extra-vars "images_to_build=${params.IMAGES_TO_BUILD}" \
                            -v
                    """
                }
            }
        }
        
        stage('Security Scanning') {
            when {
                expression { params.PUSH_TO_REGISTRY }
            }
            steps {
                script {
                    echo "🔍 Images pushed to registry"
                    echo "📊 Built images for: ${params.IMAGES_TO_BUILD}"
                    echo "🏷️ Image tag: ${IMAGE_TAG}"
                    echo "📤 Images available at ${DOCKER_REGISTRY}/${DOCKER_PROJECT}"
                }
            }
        }
        
        stage('Trigger Infrastructure Update') {
            when {
                allOf {
                    expression { params.PUSH_TO_REGISTRY }
                    equals expected: 'all', actual: params.IMAGES_TO_BUILD
                }
            }
            steps {
                script {
                    echo "🔄 Triggering infrastructure update with new images..."
                    
                    // Trigger the infrastructure update job
                    build job: 'Infrastructure/Infrastructure-Update',
                          parameters: [
                              string(name: 'IMAGE_TAG', value: "${IMAGE_TAG}"),
                              booleanParam(name: 'RESTART_SERVICES', value: true),
                              string(name: 'UPDATE_REASON', value: "New images built: ${IMAGE_TAG}")
                          ],
                          wait: false
                }
            }
        }
    }
    
    post {
        always {
            script {
                echo "📊 Build Summary:"
                echo "Images Built: ${params.IMAGES_TO_BUILD}"
                echo "Image Tag: ${IMAGE_TAG}"
                echo "Force Rebuild: ${params.FORCE_REBUILD}"
                echo "Pushed to Registry: ${params.PUSH_TO_REGISTRY}"
                
                // Archive build artifacts
                archiveArtifacts artifacts: '**/Dockerfile*', allowEmptyArchive: true
                
                // Clean workspace
                cleanWs()
            }
        }
        
        success {
            script {
                echo "✅ Jenkins Images Build Pipeline Completed Successfully!"
                
                // Send notification if configured
                if (env.SLACK_WEBHOOK) {
                    slackSend(
                        channel: '#infrastructure',
                        color: 'good',
                        message: """
                            ✅ Jenkins Images Build Successful
                            
                            📦 Images: ${params.IMAGES_TO_BUILD}
                            🏷️ Tag: ${IMAGE_TAG}
                            📤 Registry: ${DOCKER_REGISTRY}/${DOCKER_PROJECT}
                            ⏱️ Duration: ${currentBuild.durationString}
                            
                            🔗 Build: ${env.BUILD_URL}
                        """.stripIndent()
                    )
                }
            }
        }
        
        failure {
            script {
                echo "❌ Jenkins Images Build Pipeline Failed!"
                
                // Send failure notification
                if (env.SLACK_WEBHOOK) {
                    slackSend(
                        channel: '#infrastructure',
                        color: 'danger',
                        message: """
                            ❌ Jenkins Images Build Failed
                            
                            📦 Images: ${params.IMAGES_TO_BUILD}
                            🏷️ Tag: ${IMAGE_TAG}
                            ⏱️ Duration: ${currentBuild.durationString}
                            
                            🔗 Build: ${env.BUILD_URL}
                            📋 Please check logs for details
                        """.stripIndent()
                    )
                }
            }
        }
        
        unstable {
            script {
                echo "⚠️ Jenkins Images Build Pipeline Unstable!"
            }
        }
    }
}
